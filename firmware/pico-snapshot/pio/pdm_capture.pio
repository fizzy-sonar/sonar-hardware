; One SM side-sets clock and samples data after each stable half-cycle.  The SDK
; config sets out_base=PDM_CLK_GPIO, in_base=PDM_DATA_GPIO, autopush=24 bits.
.program pdm_sample
.side_set 1
wrap_target
    nop side 1 [1]
    in pins, 12 side 1
    nop side 0 [1]
    in pins, 12 side 0
wrap
