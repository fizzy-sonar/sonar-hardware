# 2026-08-27 — codex/terra-t017 — T-017 Zener deep port

## What happened
T-017 (mid tier, network authorized): gave diodeinc/pcb "Zener" its fair deep
evaluation. pcbc auto-upgraded 0.4.36 → 0.4.38 at first run on the host.

**Registry answer (T-005's open question):** the component registry is
ACCOUNT-gated, not just network-gated. `code.diode.computer/diode/registry`
returns 401/403 anonymously; `pcb component search` demands `pcb auth login`;
the local index sqlite exists but is empty. Per project rules (no accounts),
all ICs were hand-written. Exception: `pcb bom`'s stock/price data works
anonymously. Public GitHub repos (diodeinc/example especially) were the
useful references.

**Built** (`spikes/eda-zener/deep-port/sonar_v1_deep_port/`): components
AP63301 (Diodes DS42002), OPA4171 (TI SBOS516H — NOT the ticket-suggested
TLV2474: the legacy tile runs on ±5 V rails, TLV2474 is 6 V max; the KiCad
tile names no MPN at all), DRV8876 (TI SLVSEY0, stretch target done);
modules power_supply / rx_preamp_tile / tx_drive, all ported net-for-net
from `kicad-cli sch export netlist` extractions. `pcb build` ✓ 67
components; `pcb layout` ✓ 67 footprints; `pcb bom` live-priced
(US $8.68 / Global $3.82, 75% unique matched).

**Parity:** node-for-node exact vs the KiCad netlists on every checked net
(power rails, U61 internal nodes, preamp ch0, all 17 DRV8876 pins incl. EP).
Found and fixed a real error in the T-005 hand-port (4.7 nF is an output
shunt after the 49.9 Ω, not a feedback cap).

**ERC-equivalent verified with a negative test:** injected a wrong rail
check → `Error: Voltage range 3.3V 2% of +3V3 is not within 5V 10%`, build
fails; reverted → green.

**Importer re-test on host:** still an empty stub on power_supply.kicad_sch
(same "Part group 12V" error). Adoption = re-authoring, no migration path.

**Verdict: GO-WITH-CONDITIONS for v2** (conditions: account/registry
decision, toolchain pinning, multi-unit-symbol workaround, no importer, JLC
house table for D009). v1 stays KiCad per D008. Papercut list (12 items) in
REPORT.md — the multi-unit KiCad symbol one (duplicate `+`/`-` pin names
across units; fixed by renaming pins in the vendored symbol copy) is the one
worth upstreaming.

## Gotchas for the next agent
- `pins=` keys are KiCad pin NAMES; unnamed pins fall back to number.
- Multi-symbol `.kicad_sym`: `Symbol(library=..., name="Parent")`.
- Strip `Sim.*` properties from vendored KiCad symbols or Component()
  tries to resolve `${KICAD9_SYMBOL_DIR}/Simulation_SPICE.sp` and dies.
- Net names: no `.`; no duplicate `Net("x")` literals (bind to a variable).
- `datasheet=`/`description=` are Component kwargs, not `properties` keys.
- `pcb fmt` takes ONE path. `pcb new board` requires a fake repo URL and
  inits a nested .git (deleted it).

## State
Ticket T-017 → done. STATUS.md updated. NO commits (session rule) — all
work uncommitted: new files under spikes/eda-zener/deep-port/, REPORT.md
section, ticket Log, this journal, STATUS.md. Orchestrator commits.

---

## Addendum — session 2 (evening, AUTHENTICATED registry)

Joshua created a diode.computer account; `pcb auth` live. Re-ran the T-017
probes: `pcb component search` now returns full JSON (per-backend asset
booleans + live offers); `pcb component download` returns signed S3 URLs for
.kicad_sym/.kicad_mod/.step — **LCSC backend works, CSE backend is quota-
exhausted server-side** ("provider download quota is exhausted").

**Coverage survey** (29 v1 BOM lines, script + JSON in
deep-port/coverage/): 28/29 hit, 23/29 with symbol+footprint assets; all 10
key ICs/mics fully covered. Gaps: PSPHAQ127-270M (no-hit), HR911105A
(near-hit no assets), AO3415 (step only), 3 FRC/0603WA resistors
(metadata-only, no assets — stdlib generics cover their EDA side anyway).

**Registry swap:** component API is search+download ONLY — no in-language
`Part(mpn=)` auto-resolution (confirmed in diodeinc/pcb source,
crates/pcb-zen-core Component requires local symbol/footprint). Consumption
= download assets → vendor into repo → Component(symbol=Symbol(library=...),
footprint=File(...)). Swapped all 3 components to
components/registry/<MPN>/ assets. Pin-name deltas: AP63301 pin4 GND
(vendored PGND); OPA4171 "INA-" style (vendored "-INA"); DRV8876 zero
delta. Pin NUMBERS unchanged everywhere → `pcb build` ✓ 67 components and
git diff of layout/default.net vs the KiCad-verified session-1 netlist =
**0 net/node lines changed** (exact parity preserved). pcb bom: 79.2%
unique matched (up from 75%; OPA4171AIDR→C46269). pcb layout accepts the
legacy-v5 easyeda2kicad .kicad_mod files fine.

**Verdict:** GO-WITH-CONDITIONS for v2 strengthened — condition (a)
(account decision) drops, (c) multi-unit softens (registry symbols have
unique per-unit pin names; workaround only needed for vendored KiCad
Device: symbols). Standing conditions: toolchain pinning, no importer,
JLC house table. D008 (v1 = KiCad) unchanged.

## Gotchas for the next agent (session 2)
- `pcb component download --cse-part-ref` → quota exhausted today; use
  `--lcsc-part-number <C#>` (from the search JSON `lcsc.part_number`).
- Signed asset URLs expire in 1 h — download immediately, vendor the files.
- LCSC/easyeda2kicad footprints are legacy KiCad v5 `(module ...)` format —
  pcb build/layout accept them anyway.
- Registry LCSC symbols use unique per-unit pin names (INA-/INB-...) → no
  multi-unit rename workaround needed.
- `pcb component search` near-hits can be junk (HR911105A → DFR0125); check
  exact-MPN match before trusting assets booleans.
- Search may under-report lcsc assets: AP63301 lcsc showed SF but download
  returned a STEP URL too.

## State
Ticket T-017 stays done; ticket Log + REPORT.md session-2 section +
STATUS.md updated. NO commits (rule) — uncommitted: 3 component .zen swaps,
new components/registry/ assets (6 files + 2 STEP), coverage/ scripts +
results.json, refreshed deep-port/{bom.txt,bom.json,netlist.net},
regenerated layout/, REPORT.md, ticket, this journal, STATUS.md.
