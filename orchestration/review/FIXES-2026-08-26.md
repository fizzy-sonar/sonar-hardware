# Fixes applied to REVIEW-2026-08-26 findings — 2026-08-26 (same day)

| Finding | Status | Fix + verification |
|---|---|---|
| B1 Cmod power orphan | **FIXED** | J40.24 → `5V_CMOD` → R420 (0R, populated default, DNP-able) → `5V`; stale PWR_FLAG deleted; ERC /digital/ zero; netlist shows the feed reaching buck/boost inputs (T-011/T-012 Logs) |
| B2 TX duty not bounded | **FIXED** | `tx_nco_pwm` phase-gated duty, hard bound amplitude/256 ±1 clock per half-cycle; `tb_tx_duty_bound` runs the reviewer's exact failing config + sweeps: 0 violations (old RTL fails with 594); envelope re-derived in tx-limits.md; 13/13 TB |
| S1 XDC regen deletes append | **FIXED** | `write_xdc()` now read-modify-write from the MANUAL APPEND marker; regen-twice-diff proof |
| S2 rails.md stale | **FIXED** | power strategy recorded (carrier 5V → VU, R420 default populated, backfeed caveat as bring-up procedure); Cmod current allocation added |
| S3 TX_NSLEEP pio typo | **FIXED** | pio48 (V8) everywhere; schematic verified already correct (J40.48); stale TX-sheet text box fixed |
| S4 DRV8876 idle text | **FIXED** | 00=coast/Hi-Z, 11=brake; burst ring-down behavior stated; truth-table confirm added to CP-C list (Joshua) |
| S5 terminal excursion | **FIXED (doc) + DECISION** | tx-limits.md states J50 always sees 24 Vpp for nonzero duty; `decisions/D013` **proposed**: tweeter default, MA40S4S gated on Joshua's datasheet reading |
| S6 reset CDC | **FIXED** | per-domain async-assert/sync-release (`reset_sync.sv`) + btn[0] debounce (`button_debounce.sv`); `tb_reset_sync` |
| S7 no functional sim of Xilinx path | **FIXED** | functional behavioral IDDR/ODDR/BUFG/MMCM models; `pdm_clock_7series` now in the build with waveform TB (`tb_pdm_clock`); 17/17 TB; xsim/UNISIM equivalence listed for the Vivado host |
| S8 OE/mux CDC | **FIXED** | buffer_oe synchronized into selected-clock domain; BUFGCTRL selects registered per Xilinx guidelines |
| S9 FT_SIWU floating | **FIXED** | R411 10k pull-up to +3.3V; netlist FT_SIWU = {J41.15, R411.2} |
| S10 coupon quantities | **FIXED** | order-package internally consistent (all 5 boards/variant assemblable + spares) |
| S11 100nF C0G 0603 | **FIXED** | → 100 nF X7R 0603 + rationale; CP-BM checklist step added |
| NIT1 /tmp-dependent gate | **FIXED** | reference XDC committed (`orchestration/reference/`, sha256-pinned); gate passes offline |
| NIT4 ruff drift | **FIXED** | ruff==0.14.0 pinned + uv.lock refreshed; `check_coupons.sh` exit 0 on host |
| NIT3 dead wire | **FIXED** | removed |
| NIT5 ft_d idle float | **FIXED** | FPGA drives continuously |
| NIT2 zero spare I/O, NIT6 FIFO padding, NIT7 fabric→BUFG, NIT8 stale PCB | **NOTED** | documented; NIT7 on the Vivado-host checklist; NIT8 resolves when T-010 resyncs the PCB |

Re-verified by orchestrator on main: `make test` 17/17 PASS, `check_pinmap_vs_xdc.py` 44/44,
`check_ports.py` 28 ports/73 bits, `check_coupons.sh` exit 0, project ERC 346 (all legacy sheets).

**Still Joshua's (unchanged):** CP-B/CP-BM reviews, D013 ratification, datasheet readings
(MA40S4S Table 1, DRV8876 straps, CDCLVC1112 pin map), T-007 purchases, unsandboxed DRC habit.
